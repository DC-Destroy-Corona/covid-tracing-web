import React, { Fragment } from 'react';
import './SideBar.css';

const SideBar = ({}) => {
    return (
        <Fragment>
            <div>
            </div>
        </Fragment>
    )
}

export default SideBar;